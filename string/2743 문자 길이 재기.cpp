#include <iostream>
#include <algorithm>
#include <queue>
using namespace std;

string arr;

int main()
{
	cin.tie(NULL);
	ios::sync_with_stdio(false);

	cin >> arr;
	cout << arr.length();

	return 0;
}